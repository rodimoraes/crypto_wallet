<p id="notice"><%= notice %></p>

<h1>Mining Types</h1>

<table class="table table-hover" >
  <thead class="thead-light">
    <tr>
      <th><%= MiningType.human_attribute_name(:description)%></th>
      <th><%= MiningType.human_attribute_name(:acronym)%></th>
      <th colspan="3"></th>
    </tr>
  </thead>
  
  <tbody>
    <% @mining_types.each do |mining_type| %>
      <tr>
        <td><%= mining_type.description %></td>
        <td><%= mining_type.acronym %></td>
        <td><%= link_to t('links.show'), mining_type %></td>
        <td><%= link_to t('links.edit'), edit_mining_type_path(mining_type) %></td>
        <td><%= link_to t('links.delete'), mining_type, method: :delete, data: { confirm: I18n.t('message.delete') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('links.New Mining Type'), new_mining_type_path %>
|
<%=link_to "Home", root_path %>